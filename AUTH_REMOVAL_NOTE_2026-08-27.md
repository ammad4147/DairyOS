# Local Dashboard Mode

DairyOS local farm deployment opens directly to the Dashboard and does not require an application login. Authentication modules are retained as dormant compatibility code for now; they are not mounted by the application shell or runtime API.

The Dashboard component/layout is unchanged. This mode removes the login screen, authentication bootstrap, global authorization enforcement, and production admin-password startup gate.
